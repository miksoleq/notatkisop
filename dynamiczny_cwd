dynamiczny cwd
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main() {
    char *cwd = getcwd(NULL, 0);
    if (!cwd) {
        perror("getcwd");
        return 1;
    }
    printf("Current working directory: %s\n", cwd);
    free(cwd);  // bardzo ważne!
    return 0;
}
